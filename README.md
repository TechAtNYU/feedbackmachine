# Tech@NYU FEEDBACKMACHINE
==========================

## Deployment

Get access from Jeremiah so that you can deploy to heroku.
Then do 'git remote add heroku git@heroku.com:tnyu-feedbackmachine.git'.
To deploy: 'git push heroku master'